# Travel Website Frontend

Personal travel atlas built with Next.js and Mapbox routes.

## Development

```bash
npm run dev
```

The app expects `NEXT_PUBLIC_MAPBOX_TOKEN` in `.env.local`.

## Data

Travel locations, route segments, photos, and manually selected cover photos live in `lib/travelData.ts`.
